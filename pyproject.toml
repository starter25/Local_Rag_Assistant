[project]
name = "local-rag-assistant"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
    "chromadb",
    "fastapi",
    "pydantic",
    "pypdf",
    "python-docx",
    "python-multipart",
    "requests",
    "uvicorn[standard]",
]
